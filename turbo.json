{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".quasar/**", "build/**"]
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
